/**
 * Root application component.
 * 
 * Renders the main dashboard page which displays the fleet management interface.
 */

import { DashboardPage } from "./pages/DashboardPage";

export default function App() {
  return <DashboardPage />;
}
